[Unit]
Description=CareerOS encrypted local backup
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/careeros
ExecStart=/usr/bin/bash scripts/backup.sh
UMask=0077
Nice=10
IOSchedulingClass=idle
